#!/bin/sh

set -e

LOGPOINTS_HOME="${LOGPOINTS_HOME:-"$(dirname "$0")/.."}"

java -Dorg.slf4j.simpleLogger.defaultLogLevel=trace -XX:+UseSerialGC -cp "$LOGPOINTS_HOME/lib/*" anana5.sense.logpoints.PrintGraph "$@"
